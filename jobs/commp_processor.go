package jobs

func BuildCarFileFromBucket() {

	// grab all CIDs from database given the bucket and build a CAR.

	// put the CAR on the node and compute for commitment piece
}

func ComputeCommitmentPiece() {

	//commpcid, carSize, size, err := filclient.GeneratePieceCommitmentFFI(ctx, c, nd.Blockstore)
}
